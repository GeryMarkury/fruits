"use client";

import React from "react";
import Image from "next/image";

const CustomButton = () => {
	return (
		<button
			type="button"
			onClick={() => {}}
		></button>
	);
};

export default CustomButton;
